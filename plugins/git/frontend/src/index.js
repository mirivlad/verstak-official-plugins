/* Git repository descriptors: Deal-scoped, syncable, credential-free. */
(function () {
  'use strict';
  var PLUGIN_ID = 'verstak.git'; var DATA = 'repositories'; var LOCAL = 'checkouts';
  var UUID = /^[0-9a-f]{8}-[0-9a-f]{4}-[1-8][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i;
  var SECRET = /^verstak-secret:\/\/[^\s)]+$/;
  var CSS = '.git-root{height:100%;overflow:auto;background:var(--vt-color-background,#101020);color:var(--vt-color-text-primary,#f4f7fb);font-family:-apple-system,BlinkMacSystemFont,"Segoe UI",system-ui,sans-serif}.git-shell{max-width:52rem;margin:0 auto;padding:1.15rem}.git-title{font-size:1.15rem;margin:0 0 .8rem}.git-form,.git-card{display:grid;gap:.55rem;padding:.8rem;border:1px solid var(--vt-color-border,#202b46);border-radius:8px;background:var(--vt-color-surface,#15152c);margin-bottom:.65rem}.git-row{display:flex;gap:.45rem;align-items:center;flex-wrap:wrap}.git-field{display:grid;gap:.25rem;font-size:.72rem;color:var(--vt-color-text-muted,#7f8aa3)}.git-input,.git-select{box-sizing:border-box;width:100%;min-height:2rem;padding:.4rem .5rem;background:var(--vt-color-surface-input,#0f1424);border:1px solid var(--vt-color-border-strong,#2c456a);border-radius:5px;color:inherit;font:inherit;font-size:.8rem}.git-btn{font:inherit;font-size:.78rem;padding:.42rem .62rem;border:1px solid var(--vt-color-border-strong,#2c456a);border-radius:5px;background:var(--vt-color-surface-hover,#1b2440);color:inherit;cursor:pointer}.git-btn.primary{background:var(--vt-color-accent,#4ecca3);border-color:var(--vt-color-accent,#4ecca3);color:#101827}.git-meta,.git-empty,.git-error{font-size:.78rem;color:var(--vt-color-text-muted,#7f8aa3)}.git-error{color:#ffc6ce}@media(max-width:600px){.git-shell{padding:.8rem}}';
  function text(v) { return v == null ? '' : String(v); } function now() { return new Date().toISOString(); }
  function identifier() { return 'git-repository:' + Date.now() + ':' + Math.random().toString(36).slice(2, 10); }
  function tr(api, key, fallback) { return api.i18n && api.i18n.t ? api.i18n.t(key, null, fallback) : fallback; }
  function el(tag, attrs, children) { var n = document.createElement(tag); Object.keys(attrs || {}).forEach(function (k) { var v = attrs[k]; if (k === 'className') n.className = v; else if (k === 'textContent') n.textContent = v; else if (k === 'value') n.value = v; else if (k.slice(0, 2) === 'on') n.addEventListener(k.slice(2).toLowerCase(), v); else n.setAttribute(k, v); }); (children || []).forEach(function (c) { if (c != null) n.appendChild(typeof c === 'string' ? document.createTextNode(c) : c); }); return n; }
  function styles() { if (document.getElementById('git-style')) return; var s = document.createElement('style'); s.id = 'git-style'; s.textContent = CSS; document.head.appendChild(s); }
  function normalize(raw) { raw = raw || {}; var workspaceId = text(raw.workspaceId).trim(); return { id: text(raw.id).trim() || identifier(), workspaceId: workspaceId, name: text(raw.name).trim(), remoteUrl: text(raw.remoteUrl).trim(), defaultBranch: text(raw.defaultBranch).trim() || 'main', credentialRef: SECRET.test(text(raw.credentialRef).trim()) ? text(raw.credentialRef).trim() : '', updatedAt: text(raw.updatedAt).trim() || now() }; }
  function valid(record) { return UUID.test(record.workspaceId) && record.name && record.remoteUrl && /^https?:\/\/|^ssh:\/\/|^[\w.-]+@[\w.-]+:/.test(record.remoteUrl); }
  function load(api) { return api.storage.data.readNDJSON(DATA).then(function (rows) { var ids = {}; return (Array.isArray(rows) ? rows : []).map(normalize).filter(function (row) { if (!valid(row) || ids[row.id]) return false; ids[row.id] = true; return true; }); }); }
  function persist(api, rows) { return api.storage.data.writeNDJSON(DATA, rows.map(normalize).filter(valid)); }
  function list(api, args) { var scope = args && args.scope; if (!scope || scope.kind !== 'deal' || !UUID.test(text(scope.workspaceId))) return Promise.reject(new Error('DealScope.workspaceId is required')); return load(api).then(function (rows) { return rows.filter(function (row) { return row.workspaceId === scope.workspaceId; }); }); }
  function add(api, args) { var scope = args && args.scope || {}; var record = normalize(Object.assign({}, args || {}, { workspaceId: scope.workspaceId, updatedAt: now() })); if (!valid(record)) return Promise.reject(new Error('valid Deal scope, name and remote URL are required')); return load(api).then(function (rows) { if (rows.some(function (row) { return row.id === record.id; })) throw new Error('repository id already exists'); return persist(api, rows.concat([record])); }).then(function () { return record; }); }
  function update(api, args) { var record = normalize(args); if (!valid(record)) return Promise.reject(new Error('valid repository descriptor is required')); return load(api).then(function (rows) { var found = false; var next = rows.map(function (row) { if (row.id !== record.id || row.workspaceId !== record.workspaceId) return row; found = true; return record; }); if (!found) throw new Error('repository not found'); return persist(api, next); }).then(function () { return record; }); }
  function remove(api, args) { var id = text(args && args.id).trim(); var scope = args && args.scope || {}; return list(api, { scope: scope }).then(function (current) { if (!current.some(function (row) { return row.id === id; })) throw new Error('repository not found'); return load(api).then(function (all) { return persist(api, all.filter(function (row) { return row.id !== id; })); }); }).then(function () { return { id: id, removed: true }; }); }
  // Checkout paths are explicitly device-local and intentionally have no sync declaration.
  function localCheckouts(api) { return api.storage.data.readNDJSON(LOCAL).then(function (rows) { return Array.isArray(rows) ? rows : []; }); }
  function scope(props) { var n = props && props.workspaceNode || {}; var id = text(props && (props.workspaceId || n.id)).trim(); return UUID.test(id) ? { workspaceId: id, name: text(props && (props.workspaceName || n.name)) } : null; }
  var GitView = { mount: function (container, props, api) { styles(); var deal = scope(props); var disposed = false, rows = [], error = '', editing = false;
    function refresh() { return deal ? list(api, { scope: { kind: 'deal', workspaceId: deal.workspaceId } }).then(function (next) { rows = next; error = ''; render(); }) : Promise.resolve(render()); }
    function render() { if (disposed) return; container.innerHTML = ''; var shell = el('div', { className: 'git-shell' }); shell.appendChild(el('h1', { className: 'git-title', textContent: tr(api, 'ui.title', 'Git repositories') })); if (!deal) { shell.appendChild(el('div', { className: 'git-empty', textContent: tr(api, 'ui.missingDeal', 'Select a Deal to manage its repositories.') })); container.appendChild(shell); return; } if (editing) { var name = el('input', { className: 'git-input', value: '' }), remote = el('input', { className: 'git-input', value: '' }), branch = el('input', { className: 'git-input', value: 'main' }), secret = el('select', { className: 'git-select' }); secret.appendChild(el('option', { value: '', textContent: tr(api, 'ui.none', 'No credential reference') })); Promise.resolve(api.secrets.list()).then(function (items) { items.forEach(function (item) { secret.appendChild(el('option', { value: item.id, textContent: item.title || item.id })); }); }); function field(label, input) { return el('label', { className: 'git-field' }, [el('span', { textContent: label }), input]); } var form = el('div', { className: 'git-form' }, [field(tr(api,'ui.name','Name'),name), field(tr(api,'ui.remote','Remote URL'),remote), field(tr(api,'ui.branch','Default branch'),branch), field(tr(api,'ui.credential','Credential secret'),secret)]); form.appendChild(el('button', { className:'git-btn primary', type:'button', textContent:tr(api,'ui.save','Save'), onClick:function () { var secretID = secret.value; var ref = secretID ? api.secrets.copyLink(secretID).then(function (link) { var match = /\((verstak-secret:\/\/[^)]+)\)/.exec(link); return match ? match[1] : ''; }) : Promise.resolve(''); ref.then(function (credentialRef) { return add(api, { scope:{kind:'deal',workspaceId:deal.workspaceId}, name:name.value, remoteUrl:remote.value, defaultBranch:branch.value, credentialRef:credentialRef }); }).then(function () { editing=false; return refresh(); }).catch(function () { error=tr(api,'ui.error','Could not save the repository.'); render(); }); } })); shell.appendChild(form); } else shell.appendChild(el('button', { className:'git-btn primary', type:'button', textContent:tr(api,'ui.add','Add repository'), onClick:function(){ editing=true; render(); } })); if (error) shell.appendChild(el('div', { className:'git-error', textContent:error })); if (!rows.length && !editing) shell.appendChild(el('div', { className:'git-empty', textContent:tr(api,'ui.empty','No Git repositories for this Deal.') })); rows.forEach(function (row) { var card = el('div', { className:'git-card', 'data-git-repository':row.id }, [el('strong',{textContent:row.name}),el('div',{className:'git-meta',textContent:row.remoteUrl+' · '+row.defaultBranch}),el('div',{className:'git-meta',textContent:row.credentialRef ? tr(api,'ui.credential','Credential secret') : tr(api,'ui.none','No credential reference')})]); card.appendChild(el('button',{className:'git-btn',type:'button',textContent:tr(api,'ui.remove','Remove'),onClick:function(){ remove(api,{id:row.id,scope:{kind:'deal',workspaceId:deal.workspaceId}}).then(refresh).catch(function(){error=tr(api,'ui.error','Could not save the repository.');render();}); }})); shell.appendChild(card); }); container.appendChild(shell); }
    refresh().catch(function(){ error=tr(api,'ui.error','Could not save the repository.'); render(); }); container.__gitCleanup=function(){disposed=true;}; }, unmount:function(container){if(container&&container.__gitCleanup)container.__gitCleanup();if(container)container.innerHTML='';} };
  window.VerstakPluginRegister(PLUGIN_ID,{components:{GitView:GitView},activate:function(api){return Promise.all([api.commands.register('verstak.git.list',function(args){return list(api,args);}),api.commands.register('verstak.git.add',function(args){return add(api,args);}),api.commands.register('verstak.git.update',function(args){return update(api,args);}),api.commands.register('verstak.git.remove',function(args){return remove(api,args);})]);}});
})();
